@model IEnumerable<TeamCode.Models.Entities.Project>

@{
    ViewBag.Title = "Index";
}

<h62>My Projects</h62>

<p>List of My Projects</p>


<a href='@Url.Action("CreateProject","MyProjects")'>
    <img src="https://cdn3.iconfinder.com/data/icons/files/100/237046-folder_add_plus-512.png" style="width: 66px; height: 66px;" />
</a>

    <table class="table">

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.projectName)
                </td>
                <td>
                    @*<div class="btn-group btn-group-sm" role="group" aria-label="...">
                        @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-default" })
                        @Html.ActionLink("Details", "Details", new { id = item.Id }, new { @class = "btn btn-default" })
                    </div>*@
                </td>
            </tr>
        }

    </table>

